# Excel_Template

Python module for Excel Spreadsheet Datalogging

*For use in conjuction with a premade excel sheet template.

This module will copy the template, insert values where specified, and paiste the populated spreadsheet where specified.

pip install openpyxl
